import Navigation from "../Navigation/Navigation";
import "./Header.scss";

const Header = () => {
  return (
    <div className="header">
      <h2>Photo Gallery</h2>
      <Navigation />
    </div>
  );
};

export default Header;
